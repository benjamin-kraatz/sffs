[package]
name = "sffs"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
ignore = "0.4"
mimalloc = "0.1"
owo-colors = "4.0"

[profile.release]
lto = true
codegen-units = 1
panic = 'abort'
strip = true
